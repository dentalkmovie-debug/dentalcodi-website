#!/usr/bin/env node
/**
 * postbuild-inline-css.cjs
 * 현재는 더 이상 CSS 인라인화를 하지 않음 (admin.css를 외부 로드)
 * 향후 추가 postbuild 작업이 필요하면 여기에 추가
 */
console.log('✅ Postbuild complete (no CSS inline – using external admin.css for caching)')
